import { AddSpyDirective } from './add-spy.directive';

describe('AddSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new AddSpyDirective();
    expect(directive).toBeTruthy();
  });
});

import { ComplexProjectionContentDirective } from './complex-projection-content.directive';

describe('ComplexProjectionContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ComplexProjectionContentDirective();
    expect(directive).toBeTruthy();
  });
});
